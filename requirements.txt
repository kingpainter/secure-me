# Secure Me - Home Assistant Custom Integration
# Runtime dependency
bcrypt>=4.0.0

# Development / testing dependencies
# Install with: pip install -r requirements.txt
pytest
# Pinned: this package bundles a specific Home Assistant core version for tests.
# Left unpinned, pip's resolver can land on an old release whose bundled HA core
# predates 2024.11 -- before AlarmControlPanelState and ARM_VACATION existed --
# which breaks any test that touches alarm_control_panel.alarm_state.
# IMPORTANT: HA Core has required Python 3.13 since release 2025.2, so any
# version of this package bundling a post-2024.11 HA core also requires
# Python 3.13+. This means Python 3.11/3.12 CANNOT run against a modern-enough
# HA core at all -- the CI matrix (pytest.yaml) must use Python 3.13+.
# 0.13.316 bundles HA Core 2026.2.3, requires Python >=3.13 (confirmed).
pytest-homeassistant-custom-component==0.13.316
pytest-asyncio
